<%= form_for([@puzzle.puzzle_table, @puzzle]) do |f| %>
  <% if @puzzle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@puzzle.errors.count, "error") %> prohibited this puzzle from being saved:</h2>

      <ul>
      <% @puzzle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div>
   <%= f.label :image, "Photo" %>  
   <%= f.file_field :image %>
   <%= f.hidden_field :coord_x %>
   <%= f.hidden_field :coord_y %>
 </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
